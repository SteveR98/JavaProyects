package ej.nav.iu;

import ev.femxa.kkk.Persona;

public class BuscadorPosicion {
	public static final int NUM_PERSONAS=5;
	
	/**
	 * Metodo que busca la posicion de una persona y la muestra.
	 * 
	 * @param nombre recibe el nombre que va a devolver la propia posicion.
	 * @return posicion que devuelve del nombre.
	 */
/*	public int buscarPersonaPos (String nombre)
	{	int pos_persona=-5;
		int pos =0;
		Persona persona_busq =null;
		boolean encontrado =false;
		Persona persona_aux =null;
		
		while (!encontrado && pos < NUM_PERSONAS)
		{
			persona_aux =this.listaPersonas[pos];
			if (persona_aux.getNombre().equals(nombre))
			{
				encontrado =true;
				pos_persona=pos;
			}
			else pos++;
		
		return pos;
		
		}
	}
	/**
	 * Metodo que te dan una posicion numero y te devuelven la persona que esta en esa posicion.
	 * 
	 * @param pos numero dado por usuario
	 * @return persona que te devuelve segun la posicion.
	 
	public Persona obtenerPersonaPos (int pos){
		Persona persona_pedida =null ;
		
		persona_pedida = lista_personas [pos];
		
		return persona_pedida;
	}*/
	
	
	
}
